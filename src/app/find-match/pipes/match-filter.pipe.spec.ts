import { MatchFilterPipe } from './match-filter.pipe';

describe('MatchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
